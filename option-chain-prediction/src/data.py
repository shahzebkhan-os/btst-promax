import pandas as pd


def load_option_chain(symbol: str, expiry: str) -> pd.DataFrame:
    """Placeholder loader. Replace with NSE/US chain feed."""
    return pd.DataFrame()


def load_underlying_ohlc(symbol: str) -> pd.DataFrame:
    """Placeholder loader. Replace with market data feed."""
    return pd.DataFrame()
